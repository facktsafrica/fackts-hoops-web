FACKTS 1V1 DATE + PLAYER NAME SIZE 9

This installer changes only app\one-on-one\page.tsx.

Changes on mobile:
- Match dates render at exactly 9px.
- Player names render at exactly 9px.
- The global mobile paragraph rule can no longer enlarge either line.

Preserved:
- Card dimensions and spacing
- Player positions
- COMPLETED badge
- Scores, photos and match details
- Desktop typography

HOW TO INSTALL
1. Place this extracted folder inside fackts-hoops-web.
2. Double-click APPLY-1V1-DATE-NAME-SIZE-9.cmd.
3. Run npm run dev.
4. Open http://localhost:3000/one-on-one in phone view.
5. Hard-refresh and check the cards before pushing.

The installer creates a timestamped backup automatically.

